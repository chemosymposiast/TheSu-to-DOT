# Refactored / modular version

**Next agent:** read **`../REFACTORING_HANDOFF.md`** first for current progress, files to use, and next steps.

This directory contains the **refactored copy** of `TheSu-to-DOT.py`. The **original script** is left unchanged at `../TheSu-to-DOT.py`.

- **Original (do not modify):** `../TheSu-to-DOT.py`
- **Refactored (all modularisation here):** `TheSu-to-DOT.py` in this directory

Run the refactored script from this directory (e.g. `python TheSu-to-DOT.py`) so that `bootstrap` and other packages resolve correctly. Use the backup `../TheSu-to-DOT_backup_pre_refactor.py` as the original reference when running `scripts/verify_refactor_unit.py`.

For context on the TheSu XML schema and examples of how annotated sources are used in visual analysis, see the project website at `https://thesu.io`.
